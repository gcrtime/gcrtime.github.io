前端算法小结：
1、排序
  有一个不规则的数组，将要对其进行从小打到排序
  冒泡排序：
  循环获取数组相邻的两位，进行比较，将较大的数往后移，这就叫冒泡，每次循环都会获得有一个最大的数，若有N个数，循环n-1次，就能获得规律数组
  选择排序：
  第一次循环，假设a[0]是最小值，拿a[0]和a[1]比较，若a[0]>a[1],就标点最小值的tap为1.最小值就为a[1]了，如此循环下去，就会获得最小值的位置，然后再与a[0]互相替换位置，就能获得规律数组
  
2、回文
   意思就是数组规律翻倒，最简单有效的就是现成的split('').reverse().join('')
   
3、去掉重复的值
